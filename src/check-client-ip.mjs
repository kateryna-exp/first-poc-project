// Replace this function with your own IP decision logic.
// Return false to block npm requests from this IP; any other result continues.
export async function checkClientIp(clientIp) {
  void clientIp;
  return true;
}
